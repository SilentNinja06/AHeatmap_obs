{
	"name": "spiral-shutdown-logger",
	"version": "1.0.0",
	"description": "Obsidian plugin for fast logging of shutdowns, meltdowns, panic attacks, and anxiety spirals.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"keywords": ["obsidian", "obsidian-plugin"],
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.14.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.21.5",
		"obsidian": "^1.5.7",
		"tslib": "^2.6.3",
		"typescript": "^5.4.5"
	}
}
